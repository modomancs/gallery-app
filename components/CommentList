export default function CommentList({ comments = [] }) {
  return (
    <ul>
      {comments.map((comment, index) => (
        <li key={index}>
          <p>{comment.text}</p>
          <small>{comment.date}</small>
        </li>
      ))}
    </ul>
  );
}
